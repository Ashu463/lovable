# Rebuilds the workspace UI after a refresh, when only the runId is in the URL.
query RunState($runId: ID!) {
  runState(runId: $runId) {
    runId
    projectId
    userPrompt
    projectName
    status
    stalled
    pauseEvent
    completedEvent
    failedEvent
  }
}
